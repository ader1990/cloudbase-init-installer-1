name: Cloudbase-Init build / test cve

on: [push, pull_request]

permissions:
  contents: read

jobs:
  scan:
    runs-on: ubuntu-latest
    permissions:
      security-events: write
    steps:
      - uses: intel/cve-bin-tool-action@main